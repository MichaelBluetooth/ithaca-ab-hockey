import { PtsPerGamePipe } from './pts-per-game.pipe';

describe('PtsPerGamePipe', () => {
  it('create an instance', () => {
    const pipe = new PtsPerGamePipe();
    expect(pipe).toBeTruthy();
  });
});
